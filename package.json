{
  "name": "quicknav-mv3",
  "version": "2.4.20",
  "private": true,
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node dev/check.js",
    "test": "npm run check && npm run typecheck",
    "typecheck": "tsc --noEmit",
    "verify": "node dev/sync-manifest.js && node dev/gen-scripts-inventory.js && npm test && npm run build"
  },
  "devDependencies": {
    "esbuild": "^0.27.3",
    "typescript": "^5.9.3"
  }
}
